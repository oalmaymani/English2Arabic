version: "3.2"

services:
  api:
    build: ./api
    environment:
      - CONNECTION_STRING=postgres://db/belt2-day5
    networks:
      - network-backend
      - network-frontend

  web:
    build: ./web
    environment:
      - API_URL=http://api:3001/data
    depends_on:
      - api
    ports:
      - "80:80"
    networks:
      - network-frontend

networks:
  network-backend:
  network-frontend:

